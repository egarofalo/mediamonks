<nav class="navbar navbar-expand-md navbar-dark bg-dark">

    <div class="container-fluid">

        <a class="navbar-brand" href="{{ path('home') }}">MediaMonks</a>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar-primary" aria-controls="navbar-primary" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>  

        <div class="collapse navbar-collapse" id="navbar-primary">
            <ul class="navbar-nav mr-auto">

                {% if is_granted('ROLE_ADMIN') %}

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownPosts" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Posts
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdownPosts">
                            <a class="dropdown-item" href="{{ path('admin_post_create') }}">Create</a>
                            <a class="dropdown-item" href="{{ path('admin_posts_list') }}">List</a>
                        </div>                        
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownTags" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Tags
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdownTags">
                            <a class="dropdown-item" href="{{ path('admin_tag_create') }}">Create</a>
                            <a class="dropdown-item" href="{{ path('admin_tags_list') }}">List</a>
                        </div>                        
                    </li>                 
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('logout') }}">
                            Salir
                        </a>
                    </li> 

                {% else %}

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_login') }}">
                            Login
                        </a>
                    </li>

                {% endif %}

            </ul>
            {% if app.user %}
            <span class="navbar-text">
                {{ app.user.username }}
            </span>
            {% endif %}
        </div>

    </div>

</nav>